tools:
  max_tool_rounds: 3
  toxinpred2:
    enabled: true
    mode: "heuristic" # "binary", "docker", "heuristic"
    binary_path: ""
    timeout_seconds: 30
    toxin_threshold: 0.50
    min_sequence_length: 10
  sequence_parser:
    min_length: 10
    max_length: 10000
    allow_extended_iupac: true
